node_modules/
.env
data/

# JSON artifacts are published to the `data` branch by CI,
# not tracked on main. The sync writes them locally as a build
# output but they must not be committed to main.
all-jobs.json
filter-counts.json
feed-parity.json
backups/
.claude/

# Local "stash on disk" snapshots created during sessions where work
# needs to be preserved alongside the active branch (e.g. after a
# `git stash` of WIP that won't be re-applied). Local-only.
.preserved-patches/
